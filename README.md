# RestServer con TypeScript y MySQL (Plantilla)

Esta es la plantilla básica de un CRUD con TypeScript y MySQL usando Sequelize.

## Notas

Construir módulos de Node con ```npm install``` antes de correr el proyecto.